# Historical Godot prototype

The active project at `D:/Prismaze` is now Unity + C#.
This directory preserves the original optics rules, 12 level definitions and
tests as migration reference. It is outside Unity's Assets folder and is never
included in Unity builds. Godot tools, exports and editor are no longer required.
Runtime fonts and audio have moved to `Assets/Resources`.
